General set up
DONE -Create the files: index.html, style.css, script.js
-Link the style.css and script.js files to the index.html files
-Link to bootstrap and to moment.js

Index.html
-Set up the structure of the page: 
    -Header at top of the page with button to next day and previous day and poss a calendar to be clicked on?
    -Grid layout for each hour slot
    -Save button beside each hour slot

script.js
-create data structure to hold all time slots against each day
-add event listeners onto all buttons:
    -next / previous day
    -Calendar?
    -Save
-Colour code time slots based on current time:
    -grey = past
    -red = present
    -green = future

style.css:
-Separate out all styling from the html files